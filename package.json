{
  "name": "harby-lukmancylyk-hasabaty",
  "version": "1.0.0",
  "private": true,
  "description": "Harby lukmançylyk hasabaty — military medical registry (Vite+React frontend, Express/SQLite backend)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c yellow,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
